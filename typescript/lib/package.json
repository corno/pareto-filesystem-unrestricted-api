{
  "name": "pareto-filesystem-unrestricted-api",
  "version": "0.1.24",
  "author": "Corno",
  "license": "Apache-2.0",
  "description": "A library providing an unrestricted API for filesystem operations, allowing manipulation of all files and directories in the file system without the constraints of a sandboxed environment. Use this API when a sandboxed directory is not sufficient.",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "pareto-core": "^0.1.0",
    "pareto-fountain-pen": "^0.4.0"
  }
}
